-- =============================================================================
-- ขั้นที่ 6 — ค่าตั้งค่าของหน้าพิมพ์ (ช่องเซ็น 4 ช่อง)
--
-- เก็บเป็นข้อความเดียวคั่นด้วย | 4 ส่วน:
--   คำนำหน้าบรรทัด | ยศ/คำนำหน้าชื่อ | ตำแหน่งใต้เส้น | รูปแบบวันที่
-- รูปแบบวันที่: blank = เว้นเส้นให้เขียนเอง, document = วันที่ของเอกสาร, none = ไม่แสดง
--
-- ค่าเริ่มต้นตามแบบฟอร์ม "ใบรายการเสนอซื้อ" ที่โรงพยาบาลใช้อยู่ — ชื่อผู้เซ็นไม่เก็บ
-- เพราะแบบฟอร์มเว้นเส้นไว้ให้เซ็นจริงบนกระดาษ
--
-- ปลอดภัยต่อการรันซ้ำ (ON CONFLICT DO NOTHING) — ไม่แตะตารางเดิมของ HOSxP
-- =============================================================================

INSERT INTO po_offer_setting (setting_key, setting_value, description) VALUES
  ('print_sign1', 'อนุมัติ||หัวหน้าเจ้าหน้าที่พัสดุ|blank',
   'ช่องเซ็นที่ 1 (ซ้ายสุด) รูปแบบ: คำนำหน้าบรรทัด|ยศ|ตำแหน่ง|blank/document/none'),
  ('print_sign2', 'ผู้รับใบรายงานเสนอซื้อ||เจ้าหน้าที่พัสดุ|blank',
   'ช่องเซ็นที่ 2'),
  ('print_sign3', '||หน.คลังยา|document',
   'ช่องเซ็นที่ 3'),
  ('print_sign4', 'ลงชื่อ||เจ้าหน้าที่แผนกคลังยา|document',
   'ช่องเซ็นที่ 4 (ขวาสุด)')
ON CONFLICT (setting_key) DO NOTHING;
