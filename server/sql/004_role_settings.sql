-- =============================================================================
-- ค่าตั้งค่าเรื่องสิทธิ์ผู้ใช้ (เพิ่มแถวว่างไว้ให้เห็นในหน้าตั้งค่าตั้งแต่ติดตั้งใหม่)
--
-- ปล่อยค่าว่างไว้โดยตั้งใจ: ตอนติดตั้งใหม่ยังไม่มีใครเป็นผู้อนุมัติ ระบบจะเปิดสิทธิ์
-- อนุมัติให้ผู้ใช้ทุกคนชั่วคราวเพื่อให้ตั้งค่าครั้งแรกได้ แล้วปิดทันทีที่ใส่รายชื่อ
-- (ดู server/src/lib/auth.ts — resolveRoleDetail)
--
-- ปลอดภัยต่อการรันซ้ำ (ON CONFLICT DO NOTHING) — ไม่แตะตารางเดิมของ HOSxP
-- =============================================================================

INSERT INTO po_offer_setting (setting_key, setting_value, description) VALUES
  ('approver_logins', '',
   'ชื่อผู้ใช้ BMS ที่อนุมัติใบเสนอซื้อและสร้างใบขอซื้อได้ (คั่นด้วย , ) — ว่าง = ทุกคนอนุมัติได้ชั่วคราว'),
  ('viewer_logins', '',
   'ชื่อผู้ใช้ BMS ที่ให้ดูได้อย่างเดียว (คั่นด้วย , )'),
  -- ต้องเป็นค่าว่างตอนติดตั้งใหม่: ถ้าใส่ 'recorder' ไว้เลย ช่วงเปิดสิทธิ์ชั่วคราว
  -- จะถูกปิดตั้งแต่แรก แล้วโรงพยาบาลจะไม่มีใครอนุมัติหรือตั้งค่าได้เลย
  ('default_role', '',
   'สิทธิ์ของผู้ใช้ที่ไม่อยู่ในสองรายการข้างบน — เว้นว่าง = ผู้บันทึก (และเปิดสิทธิ์อนุมัติชั่วคราวจนกว่าจะกำหนดผู้อนุมัติ)')
ON CONFLICT (setting_key) DO NOTHING;
